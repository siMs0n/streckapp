import React from 'react';
import swishIcon from './swishicon.png';

const SwishIcon = () => {
  // eslint-disable-next-line jsx-a11y/alt-text
  return <img src={swishIcon} width={44} height={44} />;
};

export default SwishIcon;
